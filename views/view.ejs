<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View • Notion</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <style>
        .card-img-top {
            width: 100%;
            height: auto;
            object-fit: contain;
        }
        .note-text {
            white-space: pre-wrap;
        }
        .card-img-top {
        width: 100%;
        height: auto;
        max-height: 320px;
        object-fit: contain;
        }

        .note-text {
            white-space: pre-wrap;
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
            <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
                <svg class="bi me-2" width="40" height="32" aria-hidden="true">
                    <use xlink:href="#bootstrap"></use>
                </svg>
                <span class="fs-4">Main Menu</span>
            </a>
            <ul class="nav nav-pills">
                <li class="nav-item"><a href="/" class="nav-link active" aria-current="page">My Notes</a></li>
                <li class="nav-item"><a href="/add" class="nav-link">Add</a></li>
                <li class="nav-item"><a href="/about" class="nav-link">About</a></li>
            </ul>
        </header>
    </div>

    <main class="container pb-5">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-10">
                <div class="card shadow-sm">
                    <img
                        class="card-img-top img-fluid"
                        src="https://covers.openlibrary.org/b/isbn/<%= note.isbn %>.jpg"
                        alt="<%= note.title %>"
                    />

                    <div class="card-body">
                        <h1 class="h4 card-title mb-2"><%= note.title %></h1>
                        <p class="text-muted mb-3">
                            ISBN: <%= note.isbn %> · ID: <%= note.id %>
                        </p>

                        <h2 class="h6 text-uppercase text-muted mb-2">Your notes</h2>
                        <p class="card-text note-text mb-4"><%= note.note %></p>

                        <div class="d-flex justify-content-between">
                            <a href="/" class="btn btn-outline-secondary">
                                ← Back
                            </a>
                            <div class="btn-group">
                                <a href="/update/<%= note.id %>" class="btn btn-primary">
                                    Edit
                                </a>
                                <button
                                    type="button"
                                    class="btn btn-outline-danger delete-btn"
                                    data-id="<%= note.id %>"
                                >
                                    Delete
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="/script.js"></script>
</body>
</html>
